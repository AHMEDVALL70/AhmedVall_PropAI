# 📋 تقرير الإصلاحات المطبّقة على نسخة قطر (AhmedVall PropAI)

هذا الملف يوثّق نتيجة مراجعة الملاحظات الثلاث التي رفعها الزميل، والإجراء المتخذ لكل واحدة.

## 1️⃣ مفتاح Claude API مكشوف بالكود

**النتيجة: غير موجود بالنسخة الحالية (main).**

تم فحص `index.html` بالكامل بعدة طرق بحث (`sk-ant`, `apiKey`, `x-api-key`, `ANTHROPIC_API_KEY`، إلخ) — لا توجد أي نتيجة. كذلك تمت مقارنة الملف بالنسخة المنشورة فعلياً على GitHub Pages وهي مطابقة تماماً.

زر "تغيير المفتاح" (`showApiModal`) الموجود بالواجهة لا يحتوي على أي حقل إدخال مفتاح ولا تخزين — فقط يبدّل متغيّر `SECURE_MODE` بين true/false.

**احتمال:** الملاحظة كانت على نسخة/commit أقدم من المستودع (أرقام الأسطر المذكورة 1946 و2722 أكبر من طول الملف الحالي البالغ 1286 سطراً)، وربما تم إصلاحها لاحقاً. **ينصح بمراجعة سجل الـ commits على GitHub للتأكد من متى وأين كان هذا الكود موجوداً**، تحسباً لبقاء المفتاح القديم مكشوفاً بتاريخ commits سابق حتى لو حُذف من آخر نسخة (المفاتيح المسرّبة بأي commit سابق تبقى قابلة للوصول عبر تاريخ Git إلى الأبد، ولازم إبطالها من طرف Anthropic Console وليس فقط حذفها من الكود).

## 2️⃣ منطق السعر الصامت للمناطق غير المعروفة

**النتيجة: كانت موجودة فعلاً، تم إصلاحها الآن. ✅**

كان الكود:
```js
return 6500; // قيمة افتراضية متحفظة لمنطقة غير معروفة
```
يرجع رقماً بصمت دون تنبيه المستخدم أن هذا تخمين لا تقييم حقيقي.

**الإصلاح:** الدالة الآن ترجع `{price, isKnown}`. لو `isKnown === false`:
- تظهر رسالة تحذير صريحة `⚠️` بدل رسالة النجاح الخضراء
- التوست (toast) النهائي يظهر بلون تحذير مع كلمة "تقييم تخميني"
- وصف العقار بالجدول يوثّق أنها "منطقة غير موثقة ببياناتنا"

## 3️⃣ بيانات الأسعار قديمة/غير حية

**النتيجة: ليست خطأ برمجياً، بل قيد تصميم موثّق بالفعل بالمشروع.**

المشروع يذكر بوضوح بمصادر البيانات أنها من "تقرير ValuStrat Q2 2026" وليست بيانات حية. هذا سقف معروف لأي مشروع يعتمد بيانات ثابتة داخل الكود بدل API خارجي حي. الحل المقترح (ربط API مدفوع أو حكومي) خارج نطاق إصلاح سريع، ويحتاج تصميم بنية بيانات جديدة بالكامل.

## 🎁 إصلاح إضافي غير مذكور بالملاحظات

تم إصلاح خطأ حقيقي آخر أثناء المراجعة: دالة `callSecureProxy` كانت ترسل `{message, language, history}` بينما `worker.js` يتوقع `{system, messages}` بالضبط — يعني وضع "الاتصال الآمن" بالمستشار الذكي كان يفشل دائماً بخطأ HTTP 400 حتى لو كان `worker.js` منشوراً وشغالاً بشكل صحيح. تم إصلاحه والتحقق منه فعلياً بمتصفح حقيقي (الطلب الآن يُرسل بصيغة `{system, messages}` مطابقة تماماً).

---

**كل الإصلاحات أعلاه تم اختبارها فعلياً بمتصفح حقيقي (Playwright)، وليست افتراضية.**
